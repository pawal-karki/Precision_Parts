# Same-origin /api → Vite proxies to http://127.0.0.1:5147 (vite.config.js).
# Use `bun run dev` / `npm run dev` from frontend/ — runs scripts/dev-with-api.mjs (API then Vite).
# UI only: `bun run dev:web` (you must run `bun run dev:api` separately).
VITE_API_URL=/api
