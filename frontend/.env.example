# Development (Vite): use /api so vite.config.js proxies to http://localhost:5147 (run the .NET API first).
# Production / preview without proxy: full URL to your API, e.g. https://api.example.com/api
#
# VITE_API_URL=/api
# VITE_API_URL=http://localhost:5147/api
